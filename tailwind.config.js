/**
 * Legacy compatibility file. The rebuilt UI uses native CSS tokens, so no
 * Tailwind plugins or generated utility classes are required.
 */
module.exports = { content: [], theme: {}, plugins: [] };
