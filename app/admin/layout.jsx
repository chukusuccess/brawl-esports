export const metadata = {
  title: "Admin",
  description: "AfroBrawlers content administration.",
  robots: { index: false, follow: false },
};

export default function AdminLayout({ children }) {
  return children;
}
